13/02/2026
variabel -19/2/2026  registered area allocated on a memory  
low level = any language which directly interact with the hardware.
high level= any language which  do not directly interact with the hardware.
source code(.c) -compiler- assembler -processor( amd or intel )
assembler out is binary format
every processor has therir own assembler whose purpose is to write the instruction .
platform dependency= the combination of os and processor is called platform dependency
source code(.java) -java compiler(java c)- byte code(.class)-JVM (java ) - exececuted with n number of platform
byte code is platorm independent which is generated by java compiler
platform independency -
jdk1.8 download -8u261 


























16/2/2026



class is datatype 
Memory  
 the first area having space called metaspace . before (permanent generation )
metaspace : class  information is stored (Userprofile)
static variable :username or non static 
main()
static and non static initialy stored in metaspace . we cannot 
 
stack : is a class in java (memory area ) the main purpose is to execute the methods.
 the first method in stack is main()
 jvm will create frame for the exceution . frame includes 
 local variables always stored in stack 
 main , string[] username, current info, next info.  return type value is stored 

local variable belongs to method and static variable belongs to class.
 constant :  value is stored in constant. 




17/2/2026
println, main() -method 
system ,int, string class name, -data type
void - return type
static - key word
public - access specifier


assessment 
t20 world cup
ipl
lays
amazon
netflix
zee5
MXplayer
jiohotstar



18/2/2026


custamized array
length -is a property of array
index holding references




string amrut =teaName[0];
System.out.println(amrut);



looping statments   for each looping
for(String teaName:teaNames){
System.out.println(teaName)}


 19/2/2026
 
 
 literals - without using the new in initialization,
 int index = array references .length-1
 
 23/2/2026
 method syntaxa
 <acess specifier>returnType ,methodName
 
 
 27/02/2026
 invoking method in java is a statment.
 method : is a group of java statments which is used to perform operation on references.
 
 use of method: code reusebality
 
 class Headset{
public static String getBrand(){
SOP("fetching info");
return"noise";
}
}

                                    void          
                                     primitive
                                     object                 (method signature)
method header :<acess specifier>returnType methodName(parameter)



9/3/2026

Switch statments
case1:statment1;
break;
case2:statment2;
break;